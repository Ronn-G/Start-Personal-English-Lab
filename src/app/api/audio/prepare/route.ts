import {NextResponse} from "next/server";import {AudioCacheService} from "@/server/audio/audio-cache";import {getStorageContext} from "@/server/storage";import {isRecord,readJsonBody} from "@/server/storage/api";export const runtime="nodejs";export const dynamic="force-dynamic";export async function POST(request:Request){try{const body=await readJsonBody(request,10000);if(!isRecord(body)||typeof body.text!=="string")return NextResponse.json({error:"INVALID_INPUT"},{status:400});const result=await new AudioCacheService({database:getStorageContext().database}).prepare(body.text,{voice:typeof body.voice==="string"?body.voice:undefined,speed:typeof body.speed==="number"?body.speed:undefined});return NextResponse.json(result);}catch(e){const code=String(e).includes("INVALID_TEXT")?400:String(e).includes("Abort")?504:503;return NextResponse.json({error:String(e).slice(0,120)},{status:code});}}
