"use client";
import { useEffect, useState } from "react";
import SpeakButton from "@/components/lesson/SpeakButton";
import { audioClient } from "@/lib/audio-client";
import { AUDIO_DEFAULTS, rateToKokoroSpeed } from "@/lib/audio-domain";
import type { LadderStep, PracticeTask } from "@/lib/speaking-practice";
type Summary={practiced:number;recalledWithoutHelp:number;neededAnswer:number;personalized:number;freeSpeak:number;hard:number;okay:number;easy:number;reviewIds:string[]};
type CheckResult={understandable:boolean;verdict:"clear"|"needs_small_fix"|"needs_rewrite"|"unclear";correctedSentence:string;naturalAlternative:string|null;explanationVi:string;inputHash:string;inputText:string;checkedAt:string};
type Session={id:string;lessonId:string;currentItemIndex:number;currentStep:LadderStep;status:string;drafts:Record<string,string>;checks:Record<string,CheckResult>};
type Data={session:Session|null;lessonTitle:string;tasks:PracticeTask[];empty?:boolean;summary?:Summary};
const stepLabel:Record<LadderStep,string>={read:"Read",recall:"Recall",keywords:"Keywords",personalize:"Personalize",free_speak:"Free Speak"};
export default function SpeakingPractice({lessonId,onExit}:{lessonId:string;onExit:()=>void}){
 const [data,setData]=useState<Data|null>(null),[helped,setHelped]=useState(false),[said,setSaid]=useState(false),[draft,setDraft]=useState(""),[check,setCheck]=useState<CheckResult|null>(null),[checking,setChecking]=useState(false),[error,setError]=useState("");
 async function command(action:string,extra:Record<string,unknown>={}){setError("");const r=await fetch("/api/speaking",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,lessonId,sessionId:data?.session?.id,...extra})});const json=await r.json();if(!r.ok){setError(json.error??"Could not save speaking progress.");return;}setData(json);if(action==="complete_item"){setHelped(false);setSaid(false);setDraft("");}}
 useEffect(()=>{fetch("/api/speaking",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"status",lessonId})}).then(r=>r.json()).then(setData).catch(reason=>setError(reason instanceof Error?reason.message:"Could not load speaking practice."));},[lessonId]);
 const index=data?.session?.currentItemIndex??0,task=data?.tasks[index];
 // eslint-disable-next-line react-hooks/set-state-in-effect
 useEffect(()=>{setDraft(task&&data?.session?.drafts?.[task.id]||"");setCheck(task&&data?.session?.checks?.[task.id]||null);setSaid(false);},[task,data?.session?.drafts,data?.session?.checks]);
 useEffect(()=>{if(!data?.session||data.session.currentStep!=="personalize"||!task)return;const timer=window.setTimeout(()=>{fetch("/api/speaking",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"save_draft",lessonId,sessionId:data.session!.id,draft:draft.slice(0,500)})}).catch(()=>undefined);},350);return()=>window.clearTimeout(timer);},[draft,task,lessonId,data?.session]);
 useEffect(()=>{if(!data?.session||data.session.status!=="active")return;const items=data.tasks.slice(index,index+2).map((x,i)=>({text:x.text,priority:2+i,lessonId,itemId:x.sourceItemId,sourceType:x.sourceType==="sentence_mining"?"sentence-mining" as const:x.sourceType,config:{...AUDIO_DEFAULTS,speed:rateToKokoroSpeed(x.sourceType==="shadowing"?0.78:0.82)}}));audioClient.preload(items);return()=>audioClient.cancelLesson(lessonId);},[data,index,lessonId]);
 if(!data)return <p role="status" className="p-8 text-center font-bold text-muted">Loading speaking practice…</p>;
 if(data.empty||!data.tasks.length)return <section className="p-8 text-center"><h2 className="text-2xl font-bold">No speaking sentences yet</h2><p className="my-3">This lesson does not contain enough standalone English sentences.</p><button className="font-bold text-primary" onClick={onExit}>Back to lesson</button></section>;
 if(!data.session)return <Launcher label="Start Speaking Practice" detail={`${data.tasks.length} sentences · about 5–10 minutes`} start={()=>void command("start")} back={onExit}/>;
 if(data.session.status==="completed"){const s=data.summary!;const review=data.tasks.filter(x=>s.reviewIds.includes(x.id)).slice(0,4);return <section className="rounded-3xl border-2 border-border bg-card p-8"><h2 className="text-center text-3xl font-extrabold">Session complete</h2><p className="my-3 text-center">You practiced speaking without relying on a full script.</p><div className="mx-auto grid max-w-xl grid-cols-2 gap-3 py-5 text-sm sm:grid-cols-3"><Stat label="Practiced" value={s.practiced}/><Stat label="Without help" value={s.recalledWithoutHelp}/><Stat label="Showed answer" value={s.neededAnswer}/><Stat label="Personalized" value={s.personalized}/><Stat label="Free Speak" value={s.freeSpeak}/><Stat label="Hard / Okay / Easy" value={`${s.hard} / ${s.okay} / ${s.easy}`}/></div>{review.length>0&&<div className="mx-auto max-w-xl rounded-2xl bg-highlight p-4"><p className="font-extrabold">Review next</p><ul className="mt-2 list-disc pl-5 text-sm">{review.map(x=><li key={x.id}>{x.targetPhrase||x.text}</li>)}</ul></div>}<div className="mt-6 flex flex-wrap justify-center gap-3">{review.length>0&&<button className="rounded-full border-2 border-primary px-4 py-2 font-bold text-primary" onClick={()=>void command("review")}>Review Difficult Items</button>}<button className="rounded-full bg-primary px-5 py-3 font-bold text-white" onClick={()=>void command("start_new")}>Practice Again</button><button className="px-4 py-2 font-bold text-muted" onClick={onExit}>Back to Lesson</button></div></section>}
 if(!task)return <p role="alert" className="p-8 text-center">Speaking item is unavailable.</p>;
 const activeSession=data.session,step=activeSession.currentStep,activeSteps=task.steps,next=activeSteps[activeSteps.indexOf(step)+1],stepNumber=activeSteps.indexOf(step)+1;
 const instruction:Partial<Record<LadderStep,string>>={read:"Read the sentence aloud.",recall:"Say the complete sentence aloud before showing the answer.",keywords:"Say the idea again using only these keywords. You do not need to use the exact original words.",free_speak:"Explain why this matters to you or give a real example."};
 const advanceLabel:Partial<Record<LadderStep,string>>={read:"I read it aloud",recall:"I said it",keywords:"I said it",free_speak:"I added one more sentence"};
 function saveDraft(value:string){setDraft(value.slice(0,500));}
 async function checkSentence(){if(!task)return;setChecking(true);setError("");try{const r=await fetch("/api/speaking/check-sentence",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({lessonId,sessionId:activeSession.id,sentence:draft})}),json=await r.json();if(!r.ok)throw new Error(json.error??"Could not check your sentence.");setCheck(json.result);}catch(reason){setError(reason instanceof Error?reason.message:"Could not check your sentence.");}finally{setChecking(false);}}
 return <section className="mx-auto max-w-3xl rounded-3xl border-2 border-border bg-card p-6 sm:p-10">
  <div className="mb-8 flex flex-wrap justify-between gap-2 text-sm font-bold text-muted"><span>{data.lessonTitle}</span><span className="text-right"><span className="block">Sentence {index+1} of {data.tasks.length}</span><span className="block">Step {stepNumber} of {activeSteps.length} · {stepLabel[step]}</span></span></div>
  {step==="personalize"?<div className="space-y-4"><h2 className="text-center text-3xl font-extrabold text-heading">Make it about you</h2><p className="text-center text-body">Keep the useful pattern, but change the details.<br/><strong>Say your new sentence aloud.</strong></p><div className="rounded-2xl border border-border p-4"><p className="text-xs font-extrabold uppercase text-muted">Original</p><p className="mt-1 text-xl font-bold">{task.text}</p></div>{task.personalizationQuestion&&<div><p className="text-xs font-extrabold uppercase text-muted">Question</p><p className="mt-1 text-lg font-bold">{task.personalizationQuestion}</p></div>}<div className="rounded-2xl bg-highlight p-4"><p className="text-xs font-extrabold uppercase text-muted">{task.personalization.startsWith("Say the same")?"Try this":"Useful pattern"}</p><p className="mt-1 text-xl font-bold">{task.personalization}</p></div><p className="text-center text-sm text-muted">Ideas: work · English · gaming · recording videos · daily habits</p><div><label htmlFor="personal-sentence" className="block text-sm font-extrabold text-heading">Your sentence</label><textarea id="personal-sentence" value={draft} maxLength={500} onChange={e=>saveDraft(e.target.value)} placeholder="Write a sentence that is true for you..." rows={3} className="mt-2 w-full rounded-xl border-2 border-border bg-background p-3 text-body focus:border-primary focus:outline-none"/><div className="mt-2 flex items-start justify-between gap-3"><p className="text-xs text-muted">Writing is optional. The main goal is to say the sentence aloud.</p><button type="button" className="text-sm font-bold text-primary underline" onClick={()=>saveDraft("")} disabled={!draft}>Clear</button></div><div className="mt-3 flex gap-2"><button type="button" disabled={!draft.trim()||draft.length>500||/_{3,}/.test(draft)||checking} onClick={()=>void checkSentence()} className="rounded-xl border-2 border-primary px-4 py-2 font-extrabold text-primary disabled:opacity-40">{checking?"Checking your sentence...":"Check my sentence"}</button>{check&&<button type="button" className="px-3 py-2 font-bold text-muted" onClick={()=>setCheck(null)}>Dismiss</button>}</div></div>{check&&<SentenceFeedback result={check} stale={draft.trim().replace(/\s+/g," ")!==check.inputText} applySentence={saveDraft} checkAgain={()=>void checkSentence()}/>}<button type="button" className="w-full rounded-xl bg-primary px-5 py-3 font-extrabold text-white focus:outline-2 focus:outline-offset-2" onClick={()=>setSaid(true)}>I said my sentence</button>{said&&check&&<p className="text-center text-sm font-bold text-muted">Rate how easily you could say your final sentence aloud.</p>}</div>:<><h2 className="mb-3 min-h-28 text-center text-3xl font-extrabold leading-snug text-heading">{step==="read"?task.text:step==="recall"?(helped?task.text:task.recallMask):step==="keywords"?task.keywords.join(" · "):"Add one more sentence in your own words."}</h2>{instruction[step]&&<p className="mx-auto max-w-xl text-center text-body">{instruction[step]}</p>}{step==="free_speak"&&<p className="mt-3 text-center text-body">Prompt words: {task.keywords.slice(0,4).join(" · ")}</p>}</>}
  <div className="mt-8 flex min-h-16 flex-wrap items-center justify-center gap-3">{step!=="free_speak"&&<SpeakButton text={task.text}/>} {step==="recall"&&!helped&&<button className="rounded-full border-2 border-border px-4 py-2 font-bold" onClick={()=>{setHelped(true);void command("show_answer");}}>Show answer</button>}{next?<button className="rounded-full bg-primary px-6 py-3 font-bold text-white" onClick={()=>void command("advance",{step:next})}>{advanceLabel[step]??"Continue"}</button>:step==="free_speak"&&!said?<button className="rounded-full bg-primary px-6 py-3 font-bold text-white" onClick={()=>setSaid(true)}>I added one more sentence</button>:step==="personalize"&&!said?null:<Rating enabled={!(["personalize","free_speak"] as string[]).includes(step)||said} choose={rating=>void command("complete_item",{rating,helped})}/>}<button className="px-3 py-2 font-bold text-muted" onClick={onExit}>Exit and continue later</button></div>
  {error&&<p role="alert" className="mt-4 text-center text-wrong">{error}</p>}
 </section>;
}
function SentenceFeedback({result,stale,applySentence,checkAgain}:{result:CheckResult;stale:boolean;applySentence:(value:string)=>void;checkAgain:()=>void}){const title=result.verdict==="clear"?"Your sentence is clear":result.verdict==="needs_small_fix"?"Your sentence needs a small fix":result.verdict==="unclear"?"I’m not fully sure what you mean yet.":"Your sentence can be clearer";return <div className="rounded-2xl border-2 border-border bg-card p-4" aria-live="polite"><h3 className="text-lg font-extrabold text-heading">{title}</h3>{stale&&<p className="mt-2 rounded-lg bg-highlight p-2 text-sm font-bold">Your draft changed. Check it again for updated feedback.</p>}<p className="mt-3 text-xs font-extrabold uppercase text-muted">Corrected sentence</p><p className="mt-1 text-lg font-bold">{result.correctedSentence}</p><div className="mt-2 flex flex-wrap gap-2"><button type="button" onClick={()=>applySentence(result.correctedSentence)} className="font-bold text-primary underline">Use corrected sentence</button><SpeakButton text={result.correctedSentence} label="Listen"/></div>{result.naturalAlternative&&<><p className="mt-4 text-xs font-extrabold uppercase text-muted">More natural</p><p className="mt-1 text-lg font-bold">{result.naturalAlternative}</p><div className="mt-2 flex flex-wrap gap-2"><button type="button" onClick={()=>applySentence(result.naturalAlternative!)} className="font-bold text-primary underline">Use natural version</button><SpeakButton text={result.naturalAlternative} label="Listen"/></div></>}<p className="mt-4 text-xs font-extrabold uppercase text-muted">Why</p><p className="mt-1 text-sm text-body">{result.explanationVi}</p><p className="mt-4 text-sm font-bold">Now say the corrected sentence aloud without looking.</p><button type="button" className="mt-3 font-bold text-primary underline" onClick={checkAgain}>Check again</button></div>}function Rating({enabled,choose}:{enabled:boolean;choose:(rating:"hard"|"okay"|"easy")=>void}){const options=[{value:"hard" as const,label:"Hard",help:"I needed the sentence or answer."},{value:"okay" as const,label:"Okay",help:"I could say it with some pauses."},{value:"easy" as const,label:"Easy",help:"I could say it without much help."}];return <div className="grid w-full gap-2 sm:grid-cols-3" aria-label="Self-rating">{options.map(x=><button key={x.value} disabled={!enabled} aria-label={`Rate this speaking item ${x.value}`} className="rounded-xl border-2 border-primary p-3 text-left text-primary disabled:cursor-not-allowed disabled:opacity-40 focus:outline-2 focus:outline-offset-2" onClick={()=>choose(x.value)}><strong className="block">{x.label}</strong><span className="text-xs">{x.help}</span></button>)}</div>}
function Launcher({label,detail,start,back}:{label:string;detail:string;start:()=>void;back:()=>void}){return <section className="rounded-3xl border-2 border-border bg-card p-8 text-center"><h2 className="text-2xl font-extrabold">Guided Speaking Ladder</h2><p className="my-4 text-body">{detail}</p><button className="rounded-full bg-primary px-6 py-3 font-bold text-white" onClick={start}>{label}</button><button className="ml-3 font-bold text-primary" onClick={back}>Back</button></section>}
function Stat({label,value}:{label:string;value:string|number}){return <div className="rounded-xl border border-border p-3 text-center"><strong className="block text-xl">{value}</strong>{label}</div>}




